
一、项目概述
我需要开发一个基于 Web 的可视化 OWL 本体编辑器，专门用于公路工程造价领域。

【项目名称】CostOntology Editor（造价本体可视化编辑器）

【核心目标】
让非技术用户（造价工程师、预算编制人员）能够通过图形化界面创建、编辑和查看 OWL 本体，无需手写 XML 代码。

【核心功能】

1. 图形化展示：以节点-关系图形式展示类（Class）、属性（Property）及其继承/关联关系
2. 可视化编辑：支持拖拽添加节点、连线建立关系、编辑节点属性
3. OWL 文件导入导出：支持加载现有 .owl 文件，编辑后导出为标准 OWL/RDF XML 格式
4. 中文友好：界面、类名、属性名、注释全部支持中文
5. 多项目管理：支持创建多个本体项目，分别保存和管理

【目标用户】

- 公路工程造价工程师
- 预算编制人员
- 定额管理者
- 标准规范维护人员
  二、完整项目结构

  请帮我搭建以下项目结构：

cost-ontology-editor/
├── frontend/                        # 前端应用
│   ├── public/
│   │   └── index.html
│   ├── src/
│   │   ├── main.tsx                 # 应用入口
│   │   ├── App.tsx                  # 根组件
│   │   ├── index.css                # 全局样式
│   │   ├── components/              # UI 组件
│   │   │   ├── GraphView/           # 图形化视图
│   │   │   │   ├── index.tsx        # 主渲染组件
│   │   │   │   ├── NodeRenderer.tsx # 节点渲染
│   │   │   │   ├── EdgeRenderer.tsx # 连线渲染
│   │   │   │   └── useGraph.ts      # 图形交互逻辑
│   │   │   ├── SidePanel/           # 右侧面板
│   │   │   │   ├── index.tsx        # 面板容器
│   │   │   │   ├── DetailView.tsx   # 详情视图
│   │   │   │   ├── AddView.tsx      # 添加视图
│   │   │   │   └── CodeView.tsx     # 源码视图
│   │   │   ├── Toolbar/             # 顶部工具栏
│   │   │   │   └── index.tsx
│   │   │   ├── Dialogs/             # 对话框
│   │   │   │   ├── AddClassDialog.tsx
│   │   │   │   ├── AddPropertyDialog.tsx
│   │   │   │   └── ConfirmDialog.tsx
│   │   │   └── common/              # 通用组件
│   │   │       ├── Button.tsx
│   │   │       ├── Input.tsx
│   │   │       └── Select.tsx
│   │   ├── store/                   # 状态管理
│   │   │   ├── index.ts             # 状态导出
│   │   │   ├── ontologyStore.ts     # 本体数据状态
│   │   │   ├── uiStore.ts           # UI 状态
│   │   │   └── historyStore.ts      # 撤销/重做状态
│   │   ├── services/                # 服务层
│   │   │   ├── owlParser.ts         # OWL 解析器
│   │   │   ├── owlGenerator.ts      # OWL 生成器
│   │   │   ├── apiClient.ts         # 后端 API 客户端
│   │   │   └── fileService.ts       # 文件操作服务
│   │   ├── types/                   # TypeScript 类型定义
│   │   │   ├── ontology.ts          # 本体数据类型
│   │   │   └── api.ts               # API 接口类型
│   │   ├── hooks/                   # 自定义 Hooks
│   │   │   ├── useOntology.ts
│   │   │   ├── useGraph.ts
│   │   │   └── useHistory.ts
│   │   └── utils/                   # 工具函数
│   │       ├── validators.ts
│   │       ├── formatters.ts
│   │       └── helpers.ts
│   ├── package.json
│   ├── vite.config.ts
│   ├── tailwind.config.js
│   └── tsconfig.json
│
├── backend/                         # 后端服务（Python）
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py                  # FastAPI 入口
│   │   ├── api/                     # API 路由
│   │   │   ├── __init__.py
│   │   │   ├── ontology.py          # 本体 CRUD
│   │   │   ├── file.py              # 文件导入导出
│   │   │   └── project.py           # 项目管理
│   │   ├── models/                  # 数据模型
│   │   │   ├── __init__.py
│   │   │   ├── ontology.py          # SQLAlchemy 模型
│   │   │   └── project.py
│   │   ├── services/                # 业务逻辑
│   │   │   ├── __init__.py
│   │   │   ├── owl_service.py       # OWL 解析/生成
│   │   │   └── ontology_service.py  # 本体数据服务
│   │   ├── schemas/                 # Pydantic 数据模型
│   │   │   ├── __init__.py
│   │   │   └── ontology.py
│   │   └── database.py              # 数据库连接配置
│   ├── requirements.txt
│   ├── Dockerfile
│   └── .env.example
│
├── docker-compose.yml
├── README.md
└── .gitignore
